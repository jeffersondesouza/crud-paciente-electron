const SerializedInputParser = require('./serialized-input-parser/SerializedInputParser');
const FormValidator = require('./form-validator/form-validator');

module.exports = {
  SerializedInputParser,
  FormValidator
}